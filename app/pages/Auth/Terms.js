import React, {useState} from 'react';
import {StyleSheet, View, Text} from 'react-native';

export default Terms = () => {
  return <BasePage title="terms"></BasePage>;
};

const styles = StyleSheet.create({
});
